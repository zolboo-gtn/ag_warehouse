import type { FC } from "react";

export const VehicleIdentificationTab: FC = () => {
  return (
    <form className="flex flex-col gap-y-2 p-2">
      <h5>{`Vehicle Identification`}</h5>
    </form>
  );
};
